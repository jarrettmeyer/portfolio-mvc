define("views/MainWindowView", ["backbone"], function(Backbone) {

    var MainWindowView = Backbone.View.extend({        
        
        className: ".body-content",
        
        tagName: "div"

    });

    return MainWindowView;

});
